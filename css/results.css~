.shadow {
  -webkit-box-shadow: 0 8px 17px 0 rgba(0, 0, 0, 0.1), 0 6px 20px 0 rgba(0, 0, 0, 0.09);
  -moz-box-shadow: 0 8px 17px 0 rgba(0, 0, 0, 0.1), 0 6px 20px 0 rgba(0, 0, 0, 0.09);
  box-shadow: 0 8px 17px 0 rgba(0, 0, 0, 0.1), 0 6px 20px 0 rgba(0, 0, 0, 0.09);
}

* {
    margin: 0;
    padding: 0;
    line-height: 1.5;
    font-family: 'Open Sans', sans-serif;
    color: #212121;
}

body {
    background: #f2f6fa;
}

h1 {
    font-family: 'Raleway', sans-serif;
}

h2 {
    font-family: 'Raleway', sans-serif;
    font-size: 2em;
}

p {
    font-size: 1em;
}

input[type=submit] {
    position: relative;
    top: 0px;
    left: 50%;
    transform: translateX(-50%);
    margin: 0.5em;
    padding: 1em 1.75em;
    font-size: 1em;
    font-family: 'Raleway', sans-serif;
    border: none;
    border-radius: 5em;
    background: #e6e6e6;
    color: #212121;
    transition: background 200ms, box-shadow 200ms, top 200ms;
}

input[type=submit]:hover {
    cursor: pointer;
    top: -4px;
    background: #AED581;
    box-shadow: 0px 4px 0px #689F38;
}

input[type=submit]:focus {
    outline: none;
}

input[type=submit]:active {
    top: 0px;
    box-shadow: none;
}

#header {
    visibility: visible;
    opacity: 0.0;
    width: 100%;
    padding: 1em;
}

#header img {
    position: relative;
    left: 50%;
    transform: translateX(-50%);
    width: 300px;
}

.card {
    opacity: 0.0;
    position: relative;
    box-sizing: border-box;
    margin: 0.75em;
    left: 50%;
    transform: translateX(-50%);
    padding: 2em;
    width: 100%;
    border-radius: 0.5em;
    background: #fff;
}

.card p {
    margin: 0.25em 0em;
}

.card:after {
    content: " ";
    display: block;
    height: 0;
    clear: both;
}
